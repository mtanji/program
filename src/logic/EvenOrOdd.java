package logic;

/**
 * SQL question https://www.codewars.com/kata/even-or-odd/sql
 */
public class EvenOrOdd {
    /*
        You will be given a table, numbers, with one column number.
        Return a table with a column is_even containing "Even" or "Odd" depending on number column values.

        numbers table schema
            number INT
        output table schema
            is_even STRING
     */
    // SELECT CASE WHEN number % 2 = 0 THEN 'Even' WHEN number %2 = 1 THEN 'Odd' END as is_even FROM numbers
}
